import { BioNode } from "@/components/bio-config/bio-node";
import ConfigurableEdge from "./configurable-edge";

export const nodeTypes = {
    bioNode: BioNode
};

export const edgeTypes = {
    configurable: ConfigurableEdge
};

